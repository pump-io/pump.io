<div class="dropdown btn-group span1 omega" id="messages">
  <button class="btn dropdown-toggle btn-block
                 <% if (messages.totalItems <= 0) { %>
                 disabled
                 <% } %>"
                 data-toggle="dropdown" href="#">
    <i class="icon-envelope"></i>
    <% if (messages.items.length > 0) { %>
    <span class="text-error"><%- messages.items.length %></span>
    <% } %>
  </button>
  <ul class="dropdown-menu" role="menu">
    <% _.each(messages.items, function(activity) { %>
    <% if (_.isObject(activity)) { %>
    <li>
      <small><%= activity.content %></small>
    </li>
    <% } %>
    <% }); %>
  </ul>
</div>
