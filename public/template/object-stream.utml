<div class="span9" id="object-stream">
  <% if (data.objects && data.objects.length) { %>
  <% _.each(objects, function(object) { 
     data.object = object; %>
  <%= majorObject(obj, _) %>
  <% delete data.object;
     }); %>
  <% } else { %>
  <p>No content!</p>
  <% } %>
</div>

