<div id="minor-stream">
  <% if (minor && minor.length > 0) { %>
  <% _.each(minor, function(activity) {
     activity = activity; %>
  <%= partial("minor-activity-headless") %>
  <% delete activity;
     }); %>
  <% } %>
</div>
