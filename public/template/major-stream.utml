<ul class="media-list" id="major-stream">
  <% if (activities) { %>
  <% _.each(activities, function(activity) { %>
  <%= partial("major-activity", {activity: activity, headless: headless}) %>
  <% }); %>
  <% } %>
</ul>
